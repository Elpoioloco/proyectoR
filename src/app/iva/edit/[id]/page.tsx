import IvaForm from "@/components/IvaForm";

export default function EditIvaPage() {
    return (
        <div className="flex items-center justify-center h-screen bg-gray-100">
            <IvaForm />
        </div>
    );
}